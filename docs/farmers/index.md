# Farmers

The Farmer module provides two layers:

1. **Logistics**: deposit/withdraw items using registered chests and per-item rules.
2. **Manual farming**: a server-driven AI module that can plant, harvest, pick up drops, and optionally use bonemeal.

This module is designed to work with modded crops/items by relying on configured rules rather than hardcoded vanilla assumptions.
